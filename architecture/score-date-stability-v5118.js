/* RETIRED by Sports Big Board v5.1.19.
   ScoreDateStore now owns non-regression and merge semantics directly. This file
   remains as a harmless tombstone so an older cached index cannot install a second
   score authority around the canonical store. */
(() => {
  'use strict';
  window.SBB_V5118_SCORE_STABILITY_RETIRED=true;
})();
