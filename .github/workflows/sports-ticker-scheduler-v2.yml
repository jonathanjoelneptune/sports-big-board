name: Sports Ticker Scheduler v2 (Legacy - Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy scheduler disabled
        run: |
          echo "The GitHub-native Sports Ticker cron is intentionally disabled."
          echo "Cloudflare Cron now dispatches .github/workflows/sports-ticker-refresh.yml every 20 minutes."
