/* Sports Big Board runtime deployment configuration.
   Local mode keeps apiBase empty (same origin). GitHub Pages builds replace it. */
window.SBB_CONFIG = Object.freeze({
  apiBase: "",
  deployment: "local"
});
