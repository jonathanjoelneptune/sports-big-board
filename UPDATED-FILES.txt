Sports Big Board v4.2.2 — Milestone 1 Final Hardening

Focused corrections from the second deployed Milestone stress test:
- Dev Test visually hides Game Center/Up Next/Settings while preserving their logical drawer state for test/restore operations.
- Playback stress testing separates sustained buffering health from pause/resume ownership.
- Stall/buffering telemetry creates foreground playback pressure; background full-file cache jobs defer/yield rather than competing with active playback.
- schedule-sync health is phase-aware and heartbeats during its intentional sleep interval.
- Silver/roundup/Top Plays collection media cannot become SelectedEvent or issue Game Center requests using media ids as event ids.
- Game Center all-day coverage is throttled, 429 failures create exponential provider cooldown, and fallback Highlightly prewarm is skipped when authoritative coverage exists.
- Historical UI hydration composes /api/history/ribbon + /api/history/roundups + /api/history/discovery instead of the legacy heavy /api/history/day aggregate.
- The legacy /api/history/day route remains available with component-level Server-Timing and cached operator summary data.
- Milestone telemetry now exposes Game Center provider cooldown and media playback-pressure/full-cache-yield state.

No catalog rebuild. No soundtrack re-upload.
